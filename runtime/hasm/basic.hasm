# Startup
    closure main
    call
    return

main=               # Task ()
    native show     # [ print ]

    native add      # [ print, (+) ]
    apply 40        # [ print, (+ 40) ]
    apply 2         # [ print, (+ 40 2) ]
    call            # [ print, 42 ]

    charge          # [ (print 42) ]
    call            # [  ]
    return
